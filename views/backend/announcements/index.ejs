<% title = trans.cruds.MODULE.ANNOUNCEMENT + ' - ' + companyInfo.NAME %>
<div class="container-fluid">
    <div class="page-title">
        <div class="row">
            <div class="col-sm-6 col-12">
                <h2><%= trans.cruds.MODULE.ANNOUNCEMENT %></h2>
            </div>
            <div class="col-sm-6 col-12">
                <ol class="breadcrumb">
                  <li class="breadcrumb-item"><a href="<%= '/admin/dashboard' %>"><i class="iconly-Home icli svg-color"></i> <%= trans.global.home %></a></li>
                  <li class="breadcrumb-item"><a href="<%= '/admin/announcements' %>"><%= trans.cruds.MODULE.ANNOUNCEMENT %></a></li>
                  <li class="breadcrumb-item active"><%= trans.global.list %></li>
                </ol>
            </div>
        </div>
    </div>
</div>
<div class="container-fluid">
  <div class="row">
    <div class="col-sm-12">
      <div class="card">
        
        <div class="card-header card-no-border d-flex justify-content-between align-items-center">
          <div class="">            
            <h3><%= trans.cruds.ANNOUNCEMENT.heading.announcement_list || 'announcement_list' %></h3>
          </div>
          <% if (userInfo.permissions.includes("create_announcement")) { %>
            <div class="btn-showcase d-flex justify-content-end">
                <a href="/admin/announcement/create" title="<%= trans.global.create %>" class="btn btn-pill btn-success" id="btnAddAnnouncement">
                  <%= trans.global.create %>
                </a>
            </div>
          <% } %>
        </div>
        <div class="card-body pt-0"> 
          <div class="table-responsive theme-scrollbar">
            <table class="table table-lg display announcementsTable" id=" row_create" style="width:100%">
              <thead>
                  <tr>
                    <th>#</th>
                    <th data-data="title"><%= trans.cruds.ANNOUNCEMENT.fields.title %></th>
                    <th data-data="status"><%= trans.cruds.ANNOUNCEMENT.fields.status %></th>
                    <th data-data="createdAt"><%= trans.cruds.ANNOUNCEMENT.fields.createdAt %></th>
                    <% if (userInfo.permissions.includes("view_announcement") || userInfo.permissions.includes("edit_announcement") || userInfo.permissions.includes("delete_announcement")) { %>
                      <th data-data="action"><%= trans.cruds.ANNOUNCEMENT.fields.action %></th>
                    <% } else { %>
                      <th></th>
                    <% } %>
                  </tr>
              </thead>
              <tbody>
                  <!-- Data will be loaded via AJAX -->
              </tbody>
          </table>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
<%- include('./script') %>