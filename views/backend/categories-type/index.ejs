<% title = trans.cruds.MODULE.CATEGORY_TYPE + ' - ' + companyInfo.NAME %>
<div class="container-fluid">
    <div class="page-title">
        <div class="row">
            <div class="col-sm-6 col-12">
                <h2><%= trans.cruds.MODULE.CATEGORY_TYPE %></h2>
            </div>
            <div class="col-sm-6 col-12">
                <ol class="breadcrumb">
                  <li class="breadcrumb-item"><a href="<%= '/admin/dashboard' %>"><i class="iconly-Home icli svg-color"></i> <%= trans.global.home %></a></li>
                  <li class="breadcrumb-item"><a href="<%= '/admin/categories' %>"><%= trans.cruds.MODULE.CATEGORY_TYPE %></a></li>
                  <li class="breadcrumb-item active"><%= trans.global.list %></li>
                  <!-- <li class="breadcrumb-item active"><%= trans.cruds.CATEGORY_TYPE?.heading?.category_type_list %></li> -->
                </ol>
            </div>
        </div>
    </div>
</div>
<div class="container-fluid">
  <div class="row">
    <div class="col-sm-12">
      <div class="card">
        
        <div class="card-header card-no-border d-flex justify-content-between align-items-center">
          <div class="">            
            <h3><%= trans.cruds.CATEGORY_TYPE?.heading?.category_type_list || 'trans.cruds.CATEGORY_TYPE.heading.category_type_list' %></h3>
            <!-- <p class="desc mb-0 mt-1"><%= trans.cruds.CATEGORY_TYPE?.heading?.all_the_category_type_list || 'trans.cruds.CATEGORY_TYPE.heading.all_the_category_type_list' %></p> -->
          </div>
          <!-- <% if (userInfo.permissions.includes("create_category_type")) { %>
            <div class="btn-showcase d-flex justify-content-end">
                <a href="/admin/category-type/create" title="Add Category Type" class="btn btn-pill btn-success" id="btnAddCategoryType">
                  <%= trans.cruds.CATEGORY_TYPE?.heading?.create_category_type || 'trans.cruds.CATEGORY_TYPE.heading.create_category_type' %>
                </a>
            </div> -->
          <% } %>
      </div>
        <div class="card-body pt-0"> 
          <!-- Search Bar -->
          <!-- <div class="category-filters">
            <% if (userInfo.permissions.includes("category_type_import")) { %>
              <a href="javascript:;" class="btn btn-pill btn-success" title="Import Category Type" id="btnImportCategoryType">
                <%= trans.cruds.CATEGORY_TYPE.heading.import_category_type || 'import_category_type' %>
              </a>
            <% } %>
          </div> -->
          <table class="table table-lg display categoriesTypeTable" id=" row_create" style="width:100%">
            <thead>
                <tr>
                  <!-- <th><%= trans.cruds.CATEGORY_TYPE.fields.s_no %></th> -->
                  <th>#</th>
                  <th data-data="title"><%= trans.cruds.CATEGORY_TYPE.fields.title %></th>
                  <th data-data="status"><%= trans.cruds.CATEGORY_TYPE.fields.status %></th>
                  <th data-data="createdAt"><%= trans.cruds.CATEGORY_TYPE.fields.createdAt %></th>
                  <th data-data="action"><%= trans.cruds.CATEGORY_TYPE.fields.action %></th>
                </tr>
            </thead>
            <tbody>

            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
</div>
<%- include('./script') %>
<%- include('./modal/categoryTypeImportModal') %>